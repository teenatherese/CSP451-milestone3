name: CD Pipeline

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t cloudmart-api .

      - name: Login placeholder
        run: echo "Deploy step will connect to Azure ACR/ACI here"